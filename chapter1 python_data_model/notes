1 python内置的库：collections、random

2 用实现特殊方法来利用Python数据模型的两个好处
    （1） 作为类的用户，他们不必记住标准操作的各式名称（如如何得到元素的总数，是用.size()害死.length()）
    （2） 可以更加方便地利用Python的标准库，如random.choice函数

3 切片中的::方法：card[12::13]表示从card中的第十二个元素起，每隔13个元素取值